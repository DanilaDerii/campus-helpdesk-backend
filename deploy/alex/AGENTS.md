# Alex deployment scope

This directory is owned by Alex and contains production deployment work:

- Docker Compose or deployment scripts;
- Nginx `/helpdesk/` reverse-proxy configuration;
- HTTPS and certificate-renewal configuration;
- Linux service definitions and operational documentation.

Use placeholders for secret names and configuration. Never commit credentials,
private keys, access tokens, or production `.env` files.
