# Shared proxy settings for the HelpDesk API locations (installed to /etc/nginx/snippets/).

access_log /var/log/helpdesk-nginx/access.log helpdesk if=$helpdesk_log_request;

# Native error logs can contain OAuth query strings.
error_log /dev/null;

proxy_http_version 1.1;
proxy_set_header Host              $host;
proxy_set_header X-Real-IP         $remote_addr;
proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $scheme;

proxy_connect_timeout 5s;
proxy_read_timeout    30s;
