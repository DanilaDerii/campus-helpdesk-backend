# Local development values only. Never put real production secrets in this file.
PORT=3000
HOST=127.0.0.1
NODE_ENV=development
LOG_LEVEL=info
PUBLIC_BASE_PATH=/

# Local providers read these values. Production secrets come from Azure Key Vault.
DATABASE_URL=postgresql://helpdesk:helpdesk_dev@localhost:5433/helpdesk
TEST_DATABASE_NAME=helpdesk_test
JWT_SECRET=replace-for-local-development
BREVO_API_KEY=replace-for-local-development
